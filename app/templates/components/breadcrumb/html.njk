{% macro generate(items) %}
	<ol class="breadcrumb">
		{% for item in items %}
			{% if loop.last %}
				<li class="active">{{item}}</li>
			{% else %}
				<li><a href="#">{{item}}</a></li>
			{% endif %}
		{% endfor %}
	</ol>
{% endmacro %}

